dist: trusty
language: node_js
node_js:
  - "7"
addons:
  chrome: stable
before_script:
  - npm run start &
  - ./node_modules/.bin/chromedriver --url-base=wd/hub --port=4444 &
  - sleep 8 # give background tasks some time to start up

