dist: trusty
language: node_js
node_js:
  - "7"
addons:
  chrome: stable
before_install:
  - npm run start &
  - google-chrome-stable --headless --disable-gpu --remote-debugging-port=9222 http://localhost:8008 &

